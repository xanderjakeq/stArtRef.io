import React, { Component } from 'react';
import {Link} from "react-router-dom";
import './ArtWithRef.css';

const ArtWithRef = (props) => {
    // console.log(props)
    return(
        <h1>ArtWithRef</h1>
    )
}

export default ArtWithRef;